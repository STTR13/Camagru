<?php
	$hostname="localhost";
	$username="root";
	$password="password";
	$dsn = 'mysql:host='.$hostname.';charset=utf8';
	$setup_file='database.test.sql'

	//$verbose = TRUE;

	//$db = new Database($dsn, $username, $password);
	//$db->exec(file_get_contents($setup_file));
?>
